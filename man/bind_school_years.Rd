% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind_utils.r
\name{bind_school_years}
\alias{bind_school_years}
\title{Bind and export multiple school_years}
\usage{
bind_school_years(
  base_path,
  school_years,
  file_name_prefix = "beki_scientific_use_file",
  search_cols = c("Geschlecht", "Geburtsdatum_XLSX", "Geburtsdatum_Tag",
    "Geburtsdatum_Monat", "Geburtsdatum_Jahr", "Groesse_cm", "Masse_kg", "Schulsport_AG",
    "Vereinsmitglied", "Verein_1", "Verein_2", "Verein_3", "Verein_Woche", "S20_s",
    "BPT_m", "SLJ_cm", "OLB_s", "Star_s", "Run_m_1", "Run_54_m_Runden", "Run_Pylonen",
    "Run_m_2", "Anmerkungen"),
  missing_codes = c("--wählen--", "")
)
}
\arguments{
\item{base_path}{Character scalar. Root directory containing the per-school_year
RDS files \code{<school_year>.rds} previously created by
\code{\link{read_school_year_xlsx}} (e.g. \code{"~/beki/data-raw/"}).}

\item{school_years}{Character vector. School_Year identifiers (typically years such as
\code{c("2017_18", "2018_19")}) used to construct the input RDS filenames
\code{file.path(base_path, paste0(school_years, ".rds"))}.}

\item{file_name_prefix}{Character scalar. Prefix used for the output
filenames \code{<prefix>.rds}, \code{<prefix>.csv}, and
\code{<prefix>.xlsx} written into \code{base_path}.}

\item{search_cols}{Character vector. Names of columns that are inspected to
decide whether a row contains at least one non-missing “real” value.}
}
\value{
A named list with two tibbles:
\describe{
\item{\code{all_school_years}}{Row-bound union of all school_year tables, using the
union of all columns and filling missing cells with \code{NA}.}
\item{\code{all_values}}{Subset of \code{all_school_years} restricted to rows
where at least one of the key measurement/metadata columns contains a
non-missing value.}
}
}
\description{
Reads per-school_year RDS files, binds them into a single data set, normalizes
special missing-value codes, filters out prefilled-but-empty rows, and
writes combined CSV/RDS/Excel exports.
}
\details{
This helper assumes that for each element of \code{school_years} there exists a
corresponding RDS file \code{<school_year>.rds} in \code{base_path}, containing
a single tibble for that school_year. These files are typically generated by
\code{\link{read_school_year_xlsx}}.

All character columns are first cleaned by converting the sentinel value
\code{"--wählen--"} and empty strings \code{""} to \code{NA}. The function
then keeps only those rows where at least one of a maximal set of
measurement and questionnaire variables (e.g. anthropometrics, sports
results, club membership) is non-missing, thereby removing purely
prefilled/blank rows.

A timestamp column \code{Datensatz_erstellt_am} is added and set to the
current date via \code{Sys.Date()}.

The function writes three snapshot files into \code{base_path}, using
\code{file_name_prefix} to construct the filenames:
\itemize{
\item \code{<prefix>.rds}: Filtered data as an RDS file.
\item \code{<prefix>.csv}: Filtered data as a CSV file (UTF-8).
\item \code{<prefix>.xlsx}: Filtered data as an Excel workbook via
\pkg{writexl}.
}
}
\examples{
\dontrun{
# Bind, clean, and export school_years 2017_18 and 2018_19
res <- bind_school_years(
  base_path        = "~/beki/data-raw/",
  school_years          = c("2017_18", "2018_19"),
  file_name_prefix = "beki_scientific_use_file"
)

res$all_values            # raw union of all school_years
res$relevant_cols         # search_cols for filtering
res$all_values_relevant   # filtered rows with at least one value in search_cols
}

}
